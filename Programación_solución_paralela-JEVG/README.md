cat > README.md <<'MD'
# Suma de Arreglos en Paralelo (OpenMP)

Este proyecto implementa la suma de dos arreglos `a` y `b` para generar un arreglo `c`, utilizando un `for` paralelo con OpenMP.

## Requisitos
- g++ (GCC)
- make
- OpenMP (incluido con GCC en Linux)

## Compilar
```bash
make
